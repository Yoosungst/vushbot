/*const { Discord, VoiceState, message, member, setChannel } = require('discord.js');
const channel = message.member.voice.channel;
module.exports = {
  name: 'move',
	description: 'Move users to channel from channel.',
	execute(message, args) {
        /*message.guild.members.cache.forEach(member => {
            //guard clause, early return
            if(member.id === message.member.id || !member.voice.channel) return;
            member.voice.setChannel(channel);
          });

        }

      
    if (args[0] === message.guild.channels.id && args[1] === message.guild.channels.id){
      message.guild.channels.name.args[0].member.setChannel(args[1], 'moved by a bot');

      
    }
        

  }; */

  // у меня вроде есть идея, как это сделать. но сложно кпц) я в тебя верю
  // вроде можно сделать так, чтобы ты заходил в войс канал, писал ~move 787849514865. и он тебя и свех остальных из твоего канала перемещал в 787849514865
  // сложнаааааааааааааааааааааааааааааааааааааа
  // почему нельзя просто написать /move [voice.id] [voice.id] и всё бы работало)))
  // мне хотелось бы, чтоб просто два айди канала было , пон ПОНОПОПОПООНОПООПОНОП да)